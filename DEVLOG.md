# DEVLOG — SOn 인간화 글쓰기 엔진 구축기

> 이 엔진이 어떻게 만들어졌는지의 시간순 기록. 결정과 근거, 삽질, 실측 증거를 남긴다.
> 요약: "AI 티 안 나면서 인간이 선호하는 한국어 글"을 **모델이 바뀌어도 동일하게** 뽑는 재사용 하네스.
> 판정권을 모델 감각이 아니라 결정론 게이트(exit code)로 옮긴 것이 핵심.

## 한 줄 목표 (사용자 정의)

> "AI 티가 안 나면서 인간이 선호하는 글을 작성하고 싶다. 오푸스를 쓰던 소넷을 쓰던 결과물이 Fable5와 동일하게 나와야 한다."

이 문장이 설계 전체를 규정했다. "잘 쓴 글"이 아니라 "누가 실행해도 같은 바를 넘는 글"이 목표.

---

## Phase 0 — 원래 요청과 첫 판단

사용자가 준 브리프: korean-humanizer + patina + TextHumanize + imnotai 규칙을 내장한 "30대 중반 프리랜서 작가 SOn" 페르소나로 1,780~1,920자 자전 에세이 1편.

두 갈래로 나눠서 접근하기로 결정:
1. **재사용 엔진**(하네스) — 규칙 SSoT + HARD 게이트. 글 한 편으로 끝내지 않고 매번 재현 가능하게.
2. **1호 산출물** — 그 엔진을 통과한 실제 에세이.

근거: 전역 CLAUDE.md의 "신규 하네스는 HARD 게이트 의무 + 선언≠증거" 원칙. 글쓰기도 완료 주장에 실측(글자수·금지어 카운트)이 필요하다고 봤다.

## Phase 1 — 스캐폴드와 규칙 SSoT

- `~/.claude/qa-canon/scaffold-qa.sh`로 `~/projects/son-writer` 스캐폴드 (마스터 게이트 + secrets_gate + no_vertical_stripe_gate + REQUIREMENTS/FAILURE_LOG 스텁).
- `REQUIREMENTS.md`를 SSoT로: 브리프의 규칙 중 **기계 검증 가능한 것만** HARD(R1~R8)로, 나머지(감정 곡선·MPS 의미보존)는 SOFT로 분리.
- 초기 HARD: R1 글자수, R2 빈 부사, R3 번역투, R4 금지어, R5 마크다운/이모지, R6 문장 길이 다양화, R7 디렉티브 필수, R8 문두 접속사 캡.
- 산출물 계약: `output/*.md` 첫 줄 `<!-- son: min=N max=N -->` + `제목:` 줄 + 순수 산문. 글자수는 **공백 포함·줄바꿈 제외**로 게이트가 실측.

핵심 설계: 글자수 범위를 디렉티브로 선언 → 브리프마다 바뀌어도 게이트가 그 선언을 강제(fail-closed).

## Phase 2 — 1호 에세이와 게이트 루프 (선언≠증거 실증)

- 초안 작성 → 게이트 실행 → **FAIL[R1] 1,220자** (요구 1,780~1,920). 감으로 쓰면 미달한다는 걸 게이트가 바로 잡음 (FAILURE_LOG SW001).
- 게이트 버그도 발견: 문장 분리를 줄바꿈 제거본(flat)으로 해서 문단 경계 공백이 소실 → `body.replace("\n"," ")` 기준으로 수정 (SW002).
- 장면 5곳 증축 → **1,804자 PASS, exit 0**. 1호 `output/2026-07-04-새벽-라면-상담소.md` (AI 상담 의존 자전 에세이).

## Phase 3 — 스킬화·라우팅·메모리 (언제든 재사용)

- `~/.claude/skills/son-writer/SKILL.md` 생성 (브리프→집필→게이트 루프→증거 보고 파이프라인).
- 전역 CLAUDE.md 라우팅 표에 `/son` 행 추가 (모든 세션 자동 로드).
- 영구 메모리 `project_son_writer.md` + MEMORY.md 인덱스 등재.

## Phase 4 — 문체 게이트 추가 (투표 맥락)

사용자: "사람들이 투표하는 거긴 해." → 문체 일관성이 곧 득표력.

- **R9 어미 일관성** 추가: `style=haeyo`면 -다체 문장 ≤10%, `style=dache`면 요/죠체 ≤10% (인용·도치 몫 10%). 디렉티브에 `style=` 옵션 신설.
- 문체 샘플 포인터를 SOFT에 명시: 새 글 집필 전 1호 에세이를 앵커로 읽는다 (규칙보다 실물 샘플이 재현에 우선).

판단: 문체 "프리셋 라이브러리"는 YAGNI로 보류. 어미 게이트 + 샘플 포인터만으로 드리프트는 실질 차단.

## Phase 5 — 원본 오픈소스 실검증 (사용자 지적으로 발각)

사용자: "그 오픈소스들은 설치해서 적용한 거지?" → **아니었다.** 브리프 요약만 게이트로 옮겼을 뿐, 원본 미확인 (SW003).

- 3종 클론 + `gh repo view`로 신원·라이선스 확인: korean-humanizer(7dff5b4, MIT), patina(2280f3d, MIT, 235★), TextHumanize(c536af4, **듀얼 라이선스—상업 사용 시 재확인**).
- KO 규칙 원문을 `references/`에 vendoring + `SOURCES.md` 원장(commit·license·엔진 반영 매핑).
- 원문 대조로 브리프에 없던 규칙 발견 → **R10 고빈도 AI 어휘 캡** 추가 (다양한·혁신적인·체계적인·아울러·나아가·것이다·에 대한 등 31종, 각 ≤1회·합계 ≤3회). korean-humanizer cat.12 + patina ko-language §7 근거.
- 음성 테스트로 게이트가 위반을 실제로 잡는지 검증(위반 파일 FAIL → 제거 후 PASS).

교훈: 신원검증(설치 전 출처 확인) 원칙은 코드 패키지뿐 아니라 **규칙 원문에도** 적용해야 한다.

## Phase 6 — NYT 퀴즈 실측 참고 (외부 근거)

사용자가 NYT "Who's a Better Writer: A.I. or Humans?" (2026-03-09) 링크 제공. 크롤러 차단 → **Aside 브라우저(사용자 세션)로 직접 열어** 추출.

- 실제 출처: 외부 연구 링크 0건. 블라인드 5쌍 원작 표기가 전부. AI 쪽은 5편 모두 **Anthropic Claude Opus 4.5**. 인간 원작: Blood Meridian(매카시)/Earthsea(르 귄)/Demon-Haunted World(세이건)/Wolf Hall(맨틀)/The Fish(비숍). **시(비숍)만 인간 승, 나머지 4개는 AI 승.**
- 두 가지 엔진 반영:
  - **R11** '아니라' 대구 캡 ≤2회 — Claude 승리작 4편의 공통 수사("X가 아니라 Y" + 아포리즘 클로저)가 곧 AI 시그니처. 룰 넣자마자 **1호 에세이에서 3회 검출** → 1곳 수정, 다시 exit 0.
  - **SOFT** 비숍의 물고기 원칙(추상 감정 대신 실물 감각 디테일 최소 1곳) + 아포리즘 클로저 글당 1~2곳 제한.
- `references/nyt-ai-writing-quiz-2026-03-09.md`에 요약 원장 저장(원문 미복제).

## Phase 7 — 모델 불변 계약 명문화 + 교차 모델 실증

사용자 핵심 요구: "오푸스든 소넷이든 Fable5와 동일하게 나와야 한다."

- SKILL.md에 **모델 불변 계약 4조** 추가: ①판정은 게이트만 ②집필 전 앵커 필수 ③FAIL 라인 기계적 수정 ④SOFT는 절차 체크리스트로 압축.
- 실증 시도: Sonnet 서브에이전트 → 세션한도. vgpt/codex/gemini-direct → 전부 쿼터 429 (SW004).
- **Gemini 3 Flash**(CCR API 직접 호출, thinking-블록 파싱 우회)로 성공: v1 R1 초과(1,978자) → FAIL 피드백 1회 → **1,866자 PASS**. 2호 `output/2026-07-04-지도앱-골목.md`.
- 결론: 약한/타사 모델은 루프를 더 돌 뿐, 최종 산출물은 같은 바를 넘는다. 이것이 "모델 불변"의 실체 — 재능이 아니라 게이트 수렴.

정직한 한계: HARD 게이트는 바닥선만 보장. SOFT 리듬은 편차가 남는다(실측: 1호 문장 68개 vs 2호 29개). 앵커(계약 2조)와 R 확대로 줄이는 중.

## Phase 8 — 배포와 장르 확장

- GitHub PRIVATE `VoidLight00/son-writer` push (publish 백스톱 훅 게이트+시크릿 스캔 통과).
- 실전 투입: `writing.cmdspace.work` AI 백일장(주제 "AI 시대의 인간", 마감 2026-07-19). 1호 에세이를 제출본으로 `output/submit/`에 저장.
- 장르 확장 실증: **수필 말고 소설** 요청 → 3호 `output/2026-07-04-비뚤어진-한땀.md` (AI 재봉기계가 지운 할머니의 비뚤어진 한 땀). `style=dache`로 어미 게이트 뒤집어 적용, R1·R6 4회 루프 후 **1,701자 exit 0**. 같은 엔진이 디렉티브만 바꿔 다른 장르를 커버함을 증명.

---

## 최종 규칙 지도 (R1~R11)

| id | 무엇 | 출처 |
|---|---|---|
| R1 | 글자수 범위 (디렉티브 선언) | 브리프 |
| R2 | 빈 부사 금지 | korean-humanizer |
| R3 | 번역투 금지 | korean-humanizer |
| R4 | 금지어 | korean-humanizer + 브리프 |
| R5 | 마크다운/이모지 금지 | 브리프 |
| R6 | 문장 길이 다양화 | TextHumanize |
| R7 | 디렉티브 필수 | 산출물 계약 |
| R8 | 문두 접속사 캡 | korean-humanizer |
| R9 | 어미 일관성 (haeyo/dache) | 문체 요구 |
| R10 | 고빈도 AI 어휘 캡 31종 | korean-humanizer cat.12 + patina §7 |
| R11 | '아니라' 대구 캡 | NYT 퀴즈 실측 |

## 산출물 (전부 exit 0)

- 1호 새벽-라면-상담소 (수필, haeyo, 1,804자) — Fable 5
- 2호 지도앱-골목 (수필, haeyo, 1,866자) — Gemini 3 Flash (교차 모델 실증)
- 3호 비뚤어진-한땀 (소설, dache, 1,701자) — Fable 5 (장르 확장 실증)

## Phase 9 — 모델 불변 강화 (DPTD 교차모델 A/B → 수렴 엔지니어링)

DPTD 하네스를 son-writer 엔진에 적용해 Sonnet과 Opus로 각각 가이드를 생성하고 A/B 대조(격리 디렉토리). 실측 결과:
- **게이트·구조 층은 동일**(모델 불변): 두 모델 다 8파일·비유≥4·이모지0·verify_dptd exit 0. 검증된 골격은 모델과 무관.
- **본문 층은 거의 완전히 다름**(모델 종속): main-guide 문장 겹침 2%, 전체 -20%, 비유 소재 전부 상이(놀이공원 vs 자판기 등). 자유 생성이라 모델 목소리로 새로 쓰임.

결론: 하네스는 바닥선·구조를 보장하지 단어를 보장하지 않는다. "모델 불변 100%"를 원하면 자유도를 더 깎아야 한다 → 수렴 엔지니어링 3종을 son-writer에 프리미티브로 이식:

1. **`gates/convergence.sh`** — golden 대비 표면 유사도(문자 3-gram Jaccard, 오프라인 결정론) 측정·임계 강제. selftest 통과(동일 1.0/다른 0.0/수렴 0.62). 실측 baseline: Sonnet vs Opus 가이드 0.08~0.20(발산 확인). master 게이트 자동실행 대상 아님(명시 호출 도구).
2. **`references/analogy-bank.json`** — 비유 발명→선택. concept_id별 canonical(index 0)을 두 모델이 공유 → 비유 발산 제거. 두 run이 실제 낸 검증된 비유로 초기 채움.
3. **REQUIREMENTS "모델 불변 강화 계약"** — 결정론 필드(수치·용어·claim·순서·비유선택) vs 목소리 필드(연결 표현) 경계 선언. extractive over generative. 100% 필요시 golden 동결 방식 명시.

미완(다음): DPTD explainer에 analogy-bank 강제 + golden 기준 convergence.sh 배선(현재는 son-writer 프리미티브까지).

**수렴 실증 (2026-07-07, 증거 `evidence/convergence/`):** 발산 최악 파일(feynman-explainer, 0.082)을 재현 모드 스펙(비유 뱅크 canonical 강제 + 개념 순서 고정 + 수치 추출)으로 Sonnet·Opus 각각 재생성해 측정.
- 자유 생성 0.082 → 재현 모드 **0.567 (7배↑)**. 비유 헤딩은 바이트 단위 동일해짐.
- 남은 0.43 격차 = 같은 사실의 다른 문장 표현 = 경계 계약상 "목소리 필드"(모델 허용). 발산이 아니라 설계된 자유도.
- **템플릿 티어 실측**: 문장까지 고정 스켈레톤+결정론 슬롯으로 묶자 Sonnet·Opus 산출물이 **byte-identical, convergence 1.000**. 0.70 목표 초과 달성.
- 결론: 티어로 재현성을 고른다 — 자유 생성 0.08(목소리 100%) / 재현 모드 0.57(비유·사실 고정, 문장 열림) / 템플릿 1.00(모델=조립기, 목소리 0) / golden 동결 1.00. **재현성↔목소리는 트레이드오프이고, 하네스는 그 다이얼을 티어로 노출한다.**

## 다음 루프 (미완)

- DPTD explainer 추출-조립 개조 → convergence.sh로 0.70+ 실증.
- 실제 투표/평가 결과를 FAILURE_LOG에 환류 → "인간 선호"는 게이트로 못 재므로 투표 결과만이 유일한 신호.
- SOFT 리듬 편차 축소: 게이트로 잴 수 있는 면적(R) 확대.
