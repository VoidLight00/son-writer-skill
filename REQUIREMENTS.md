# son — Requirements (SSoT)

> SOn 인간화 글쓰기 엔진의 단일 진실. 게이트는 이 표의 각 R을 exit code로 검증한다.
> 페르소나: 30대 중반 프리랜서 작가 SOn — 자조 유머 + 약간 쓸쓸 + 따뜻한 마무리.
> 출처 규칙 통합: dotoricode/korean-humanizer, devswha/patina(168패턴), ksanyok/TextHumanize, imnotai.
> 원본 규칙 파일은 `references/`에 vendored (commit·license 원장: `references/SOURCES.md`). 집필 전 SOFT 판단이 애매하면 원문을 직접 읽는다.

## 산출물 계약

`output/*.md` 형식 (게이트가 이 계약을 강제한다):

```
<!-- son: min=1780 max=1920 style=haeyo -->
제목: <제목 한 줄>

<본문 — 순수 산문>
```

- 글자수 = 본문만(디렉티브·제목 제외), 공백 포함, 줄바꿈 제외.
- min/max는 브리프마다 디렉티브로 선언한다. 디렉티브 없는 산출물은 게이트가 차단한다(fail-closed).
- style은 haeyo(기본, 존댓말 해요체) | dache(-다체 전문문서). 생략 시 haeyo — 전역 규칙(user-facing 글=해요체)과 동일 기본값.

## HARD (son_text_gate.sh가 exit code로 차단)

| id | requirement | gate |
|---|---|---|
| R1 | 글자수 min~max 범위 내 (공백 포함, 본문만) | son_text_gate.sh |
| R2 | 빈 부사 금지: 매우/굉장히/상당히/극도로 | son_text_gate.sh |
| R3 | 번역투 금지: ~을/를 통해, ~에 있어서, ~에 의해 | son_text_gate.sh |
| R4 | 금지어: 활용/극대화/시사한다/주목할 만/도모/결론적으로 | son_text_gate.sh |
| R5 | 마크다운 리스트·볼드·헤딩·이모지 금지 (순수 산문만) | son_text_gate.sh |
| R6 | 문장 길이 다양화: 25자 이하 단문 1개 이상 + 70자 이상 장문 1개 이상 | son_text_gate.sh |
| R7 | 디렉티브(`<!-- son: min= max= -->`) 필수 | son_text_gate.sh |
| R8 | 접속사 남발 금지: 문두 그리고/하지만/그러나 합계 3회 이하 | son_text_gate.sh |
| R9 | 어미 일관성: style=haeyo면 -다체 문장 ≤10%, style=dache면 요/죠체 문장 ≤10% (인용·도치 몫) | son_text_gate.sh |
| R10 | 고빈도 AI 어휘 캡: 다양한/혁신적인/체계적인/아울러/나아가/것이다/에 대한 등 31종 각 ≤1회·합계 ≤3회 (korean-humanizer cat.12 + patina §7) | son_text_gate.sh |
| R11 | "X가 아니라 Y" 대구 구문 ≤2회 — NYT 퀴즈에서 확인된 Claude 시그니처 수사 (references/nyt-ai-writing-quiz) | son_text_gate.sh |
| R12 | 전자책 조립본은 자체완결이다 — 외부 호스트 리소스·CSS @import 0건 | book_gate.sh |
| R13 | 커버리지 락: 원고 전 편이 조립본 본문(h3+문단)에 실려 있고 본문 하한 28,000자 | book_gate.sh |
| R14 | 조립본은 라이트·다크·시스템 3상태 테마를 갖는다 — 명시 규칙·시스템 규칙·토글·저장·선페인트 스크립트 전부 | book_gate.sh |

## SOFT (게이트로 못 재는 것 — 집필 시 사람이/모델이 지킨다)

- **문체 레퍼런스(SOn 결 샘플): `output/2026-07-04-새벽-라면-상담소.md`** — 새 글 집필 전 이 실물을 먼저 읽고 리듬을 맞춘다. 산문 규칙보다 샘플이 우선.
- 톤: 자조 유머 → 쓸쓸 → 따뜻한 희망 순의 감정 기복. 존댓말(해요체), -다체 금지.
- 자연스러운 반복·자기수정을 1~2곳 심는다 ("아니, 정확히는…" 류).
- 아포리즘 클로저(짧은 단정문으로 문단 마무리)는 글 전체 1~2곳까지만 — NYT 퀴즈의 Claude 승리작 공통 수사라 남발하면 역으로 AI 티.
- 비숍의 물고기 원칙: 추상 감정 문장 대신 실물 감각 디테일을 최소 1곳 (인간이 AI를 이긴 유일한 장르=시의 승부처).
- 너무 완벽하거나 논리정연한 전개 금지. 살짝 어긋나는 구어 허용.
- 의미·수치·감정 보존(MPS): 기존 글을 변환할 때 사실과 숫자를 바꾸지 않는다.
- 선호 표현 결: 쓸만하다, 느껴지더라, 나도 모르게 (강제 삽입은 아님).

## 모델 불변 강화 계약 (재현 모드 — 기술 문서·가이드용)

에세이(창작)는 모델 목소리가 갈려도 된다. 그러나 **기술 문서·가이드는 모델이 바뀌어도 거의 같아야** 할 때가 있다. 이때 적용하는 계약이다. 원리: 모델에 남긴 자유도 하나가 곧 모델 간 발산이다. 자유도를 아래처럼 분리해 결정론 필드는 코드가 소유하고, 목소리 필드만 모델에 연다.

### 필드 경계 (선언)

| 층 | 무엇 | 누가 소유 | 게이트 |
|---|---|---|---|
| 결정론 | 수치·용어·claim 집합·섹션 순서·구조·**비유 선택** | 코드/뱅크 (모델 발명 금지) | 동일성(convergence.sh) |
| 목소리 | 설명 문장의 연결 표현 | 모델 | 바닥선만(son_text_gate 금지어류) |

### 규칙

- **비유는 발명이 아니라 선택.** `references/analogy-bank.json`의 concept_id별 `canonical`(index 0)을 기본 채택한다. 뱅크에 없는 개념만 새로 만들되 즉시 canonical로 등재·커밋(다음 실행부터 결정론). 이게 "놀이공원 vs 자판기" 발산을 없애는 지점.
- **수치·용어·claim·순서는 소스에서 추출**해 고정한다. 모델이 새 숫자·새 용어·새 순서를 만들지 않는다(extractive over generative).
- **수렴 게이트로 측정·강제.** golden 참조본이 있으면 `bash gates/convergence.sh <golden> <candidate> [임계]`로 표면 유사도(문자 3-gram Jaccard, 오프라인 결정론)를 재고 임계 미달이면 재생성.
- **실측 티어별 임계 (2026-07-07, feynman-explainer Sonnet vs Opus, 증거: `evidence/convergence/`):**

| 티어 | 고정한 것 | 실측 유사도 | 임계 |
|---|---|---|---|
| 자유 생성 | 없음 | 0.082 | — |
| 재현 모드(비유뱅크+사실+구조) | 비유·수치·순서 | 0.567 | 0.55 |
| 템플릿 티어 | 위 + 문장 슬롯화 | 1.000 (byte-identical) | 0.70 |
| golden 동결 | 전부 | 1.00 | 1.00 |

  재현 모드로 발산 0.08→0.57(7배↑, 목소리 문장은 열어둠). 템플릿 티어(문장까지 스켈레톤+결정론 슬롯)는 두 모델 byte-identical(1.000) — 단, 이 티어에서 모델은 조립기이고 목소리는 0. **재현성↔목소리 트레이드오프를 티어로 고른다.**

### 100% 원하면

문장까지 100% 동일은 자유 생성으로 불가능하다. 진짜 100%가 필요하면 **한 번 생성해 golden으로 동결**하고 하네스는 재조립·convergence.sh 검증만 한다(모델이 hot path에서 빠짐). 재현 산출물 vs 재현 생성 중 무엇을 원하는지로 갈린다.

## 재사용 방법

1. 브리프 확정(주제·각도·톤·글자수 범위).
2. `output/<date>-<slug>.md`에 디렉티브+제목+본문 작성.
3. `bash gates/verify_son.sh ~/projects/son-writer` → exit 0까지 diagnose→fix 루프(≤3회, 실패는 FAILURE_LOG.md에 기록).
4. (기술 문서·재현 모드) 비유는 analogy-bank에서 선택, 수치·용어는 소스 추출, golden 대비 `convergence.sh` ≥0.80 확인.
