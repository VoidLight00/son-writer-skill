---
name: son-writer
description: SOn 인간화 글쓰기 엔진 — 30대 중반 프리랜서 작가 페르소나로 AI 티 0%의 한국어 산문(에세이/투표 페이지 글/자전 글)을 쓰고 HARD 게이트로 검증한다. "/son", "SOn으로 써줘", "인간화 글쓰기", "AI 티 없는 에세이", "사람이 쓴 것 같은 글" 요청 시 사용.
---

# SOn 인간화 글쓰기 엔진

루트: `~/projects/son-writer/`. 규칙 SSoT는 `REQUIREMENTS.md`(R1~R11 HARD + SOFT 지침). 산출물은 게이트 exit 0일 때만 완성으로 보고한다.

## 모델 불변 계약 (어떤 모델이 실행해도 동일 결과)

이 하네스는 실행 모델(Fable/Opus/Sonnet/GPT 등)의 내장 감각을 신뢰하지 않는다. 품질 판정권은 전부 결정론 게이트에 있다.

1. **판정은 게이트만.** "잘 쓴 것 같다"는 판단 금지. `verify_son.sh` exit 0이 유일한 완성 정의.
2. **집필 전 앵커 필수.** REQUIREMENTS.md 전문 + 문체 샘플(`output/2026-07-04-새벽-라면-상담소.md`)을 읽지 않고 쓰기 시작하는 것 금지. 규칙 암기가 아니라 샘플 리듬 모방이 모델 편차를 줄인다.
3. **루프는 기계적으로.** FAIL 라인은 위반 단어·횟수·요구치를 그대로 알려준다. 해석하지 말고 지목된 지점만 고치고 재실행한다. exit 0 전에는 어떤 표현으로도 완성 보고 금지.
4. **SOFT 항목은 절차로 압축.** 감각 판단이 필요한 항목(감정 곡선, 비숍 디테일, 자기수정 심기)은 구조 레시피의 해당 위치에 1개씩 기계적으로 배치한다 — 재능이 아니라 체크리스트로.

## 재현성 티어 다이얼 (문서 종류 → 권장 티어)

모델 불변과 모델 목소리는 트레이드오프다. 자유도를 없앨수록 재현성은 오르고 목소리는 준다. 문서 종류로 티어를 고른다 (전부 feynman-explainer Sonnet vs Opus 실측, 증거 `evidence/convergence/`):

| 티어 | 고정 | 교차모델 유사도 | 언제 |
|---|---|---|---|
| 자유 생성 | 없음 | 0.082 | 에세이·창작. 목소리가 자산 |
| 재현 모드 | 비유·수치·순서 | 0.567 | 설명서·가이드. 사실·구조는 같고 문장은 모델 색 |
| 템플릿 티어 | + 문장 스켈레톤·슬롯 | 1.000 (byte-identical) | 규격·계약·스펙. 누가 돌려도 글자까지 동일. 모델=조립기 |
| golden 동결 | 전부 (재생성 안 함) | 1.00 | 한 번 만든 걸 그대로 재검증만 |

## 재현 모드 (기술 문서·가이드 — 모델 바뀌어도 ~같은 본문)

에세이는 위 4조로 충분(목소리 갈려도 됨). 문서를 모델 불변으로 뽑으려면 REQUIREMENTS "모델 불변 강화 계약"을 추가 적용한다.

1. **비유는 선택.** `references/analogy-bank.json` concept_id별 canonical(index 0) 채택. 발명 금지. 없는 개념만 새로 만들어 즉시 등재·커밋.
2. **수치·용어·순서는 소스 추출.** 새 숫자·새 용어·새 순서 생성 금지(extractive).
3. **수렴 검증.** golden 참조본 대비 `bash gates/convergence.sh <golden> <candidate> [임계=0.80]`. 미달이면 재생성.
4. **100% 필요시** golden 동결 후 재조립·검증만(모델 hot path 제외). 실측: 자유 생성 교차모델 baseline 0.08~0.20 → 뱅크+추출 적용 목표 0.70+.

DPTD explainer 등 외부 하네스에 이식: 프롬프트에 analogy-bank 강제 + verify에 convergence.sh(golden 필요) 추가. son-writer가 이 프리미티브의 본가.

## 파이프라인

1. **브리프 확정** — 주제, 각도(자전/관찰/논평), 톤(기본: 자조 유머→쓸쓸→따뜻한 희망), 글자수 범위(기본 1780~1920, 공백 포함), 구조(에피소드→구체 경험→고민→독자 질문).
2. **집필** — 먼저 문체 샘플 `output/2026-07-04-새벽-라면-상담소.md`를 읽고 리듬을 맞춘 뒤, `~/projects/son-writer/output/<date>-<slug>.md`에 작성:
   ```
   <!-- son: min=1780 max=1920 style=haeyo -->
   제목: <한 줄>

   <본문 — 순수 산문, 해요체>
   ```
   style은 haeyo(기본)|dache. 어미 드리프트는 R9 게이트가 차단한다(-다체 문장 ≤10%).
   집필 중 지킬 것(HARD는 게이트가 잡지만 처음부터 지키면 루프가 준다):
   - 빈 부사(매우/굉장히/상당히/극도로), 번역투(통해/에 있어/에 의해), 금지어(활용/극대화/시사/주목할 만/도모/결론적으로) 금지.
   - 리스트·볼드·헤딩·이모지 금지. 문두 그리고/하지만/그러나 ≤3회(대신 "근데" 등 구어).
   - 문장 길이 극단 혼합: 10자 안팎 단문과 70자+ 장문을 섞는다.
   - 자기수정("아니, 정확히는…")과 자연스러운 반복을 1~2곳. 너무 논리정연하게 쓰지 않는다.
   - 존댓말 해요체. 기존 글 변환 시 의미·수치·감정 보존.
3. **게이트 루프** — `bash ~/projects/son-writer/gates/verify_son.sh ~/projects/son-writer` 실행. FAIL[son_text] 사유를 읽고 diagnose→fix→재실행 ≤3회. 실패는 `FAILURE_LOG.md`에 1행 기록.
4. **보고** — PASS 라인(글자수 실측 포함)을 증거로 첨부해 본문 전문을 전달한다. "통과했을 것"이라는 자가보고 금지.

## 재사용 변형

- 글자수 범위는 디렉티브로 브리프마다 조정 (예: 스레드용 `min=450 max=500`).
- 페르소나 톤 변형이 필요하면 REQUIREMENTS.md SOFT 절만 브리프에서 덮어쓰고 HARD는 유지.
